# Leave VITE_API_URL empty in development.
# Vite's dev server proxies /api and /uploads to http://localhost:3001 automatically.
# Set this only when deploying frontend and backend to different domains.
VITE_API_URL=
